import { ReplyTweetDetails } from './reply-tweet-details';

describe('ReplyTweetDetails', () => {
  it('should create an instance', () => {
    expect(new ReplyTweetDetails()).toBeTruthy();
  });
});
