import { Validitytokencheck } from './validitytokencheck';

describe('Validitytokencheck', () => {
  it('should create an instance', () => {
    expect(new Validitytokencheck()).toBeTruthy();
  });
});
