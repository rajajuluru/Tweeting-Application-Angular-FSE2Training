import { ScheduleFlightModal } from './schedule-flight-modal';

describe('ScheduleFlightModal', () => {
  it('should create an instance', () => {
    expect(new ScheduleFlightModal()).toBeTruthy();
  });
});
