import { BookingMasterPassengerDetails } from './booking-master-details';

describe('BookingMasterDetails', () => {
  it('should create an instance', () => {
    expect(new BookingMasterPassengerDetails()).toBeTruthy();
  });
});
