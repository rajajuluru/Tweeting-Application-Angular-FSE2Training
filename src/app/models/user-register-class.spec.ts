import { UserRegisterClass } from './user-register-class';

describe('UserRegisterClass', () => {
  it('should create an instance', () => {
    expect(new UserRegisterClass()).toBeTruthy();
  });
});
