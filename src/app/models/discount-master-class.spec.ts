import { DiscountMasterClass } from './discount-master-class';

describe('DiscountMasterClass', () => {
  it('should create an instance', () => {
    expect(new DiscountMasterClass()).toBeTruthy();
  });
});
