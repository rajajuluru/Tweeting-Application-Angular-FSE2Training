import { AirlineDetailsModal } from './airline-details-modal';

describe('AirlineDetailsModal', () => {
  it('should create an instance', () => {
    expect(new AirlineDetailsModal()).toBeTruthy();
  });
});
