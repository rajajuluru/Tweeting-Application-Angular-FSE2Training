import { FilghtSearchHelper } from './filght-search-helper';

describe('FilghtSearchHelper', () => {
  it('should create an instance', () => {
    expect(new FilghtSearchHelper()).toBeTruthy();
  });
});
