import { LoginRequestHelperClass } from './login-request-helper-class';

describe('LoginRequestHelperClass', () => {
  it('should create an instance', () => {
    expect(new LoginRequestHelperClass()).toBeTruthy();
  });
});
